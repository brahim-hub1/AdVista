import SwiftUI

enum AppFonts {
    static let screenTitle = Font.system(.largeTitle, design: .rounded).weight(.semibold)
    static let sectionTitle = Font.system(.title2, design: .rounded).weight(.semibold)
    static let cardTitle = Font.system(.headline, design: .rounded).weight(.semibold)
    static let body = Font.system(.body, design: .rounded)
    static let caption = Font.system(.caption, design: .rounded)
    static let kpiValue = Font.system(.title2, design: .rounded).weight(.bold)
}
